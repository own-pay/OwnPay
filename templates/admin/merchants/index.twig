{% extends 'admin/layout/base.twig' %}
{% block title %}Merchants — {{ app_name }}{% endblock %}
{% block content %}
<div class="op-page-header">
    <h1>Merchants</h1>
    <a href="/admin/merchants/create" class="op-btn op-btn-primary">+ Add Merchant</a>
</div>
<div class="op-card">
    <div class="op-card-body op-p-0">
        <table class="op-table">
            <thead><tr><th>ID</th><th>Business</th><th>Domain</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead>
            <tbody>
                {% for m in merchants %}
                <tr>
                    <td>{{ m.id }}</td>
                    <td><strong>{{ m.business_name }}</strong></td>
                    <td><code>{{ m.domain ?? '—' }}</code></td>
                    <td><span class="op-badge op-badge-{{ m.status == 'active' ? 'success' : 'muted' }}">{{ m.status }}</span></td>
                    <td>{{ m.created_at|date("M d, Y") }}</td>
                    <td><a href="/admin/merchants/{{ m.id }}/edit" class="op-btn op-btn-xs op-btn-outline">Edit</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
