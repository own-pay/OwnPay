# Triggers the documentation sync in own-pay/ownpay-docs whenever
# synced documentation files change on the main branch.
name: Notify Docs Sync

on:
  push:
    branches: [main]
    paths:
      - 'CHANGELOG.md'
      - 'CONTRIBUTING.md'
      - 'ROADMAP.md'
      - 'docs/ARCHITECTURE.md'
      - 'docs/FEATURES.md'
      - 'docs/LOCAL_SETUP.md'
      - 'docs/TRANSLATIONS.md'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger docs sync
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.DOCS_SYNC_TOKEN }}
          repository: own-pay/ownpay-docs
          event-type: sync-docs
