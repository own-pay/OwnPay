<IfModule mod_rewrite.c>
    RewriteEngine On
    Options -Indexes

    # Block sensitive system files
    RewriteRule ^(\.git|\.env|\.env\..+|op-config\.php|op-temp-config\.php|composer\.json|composer\.lock|README\.md)$ - [F,L,NC]

    # Block orphaned test/debug scripts (defense-in-depth)
    RewriteRule ^test_.*\.php$ - [F,L,NC]
    RewriteRule ^check_db\.php$ - [F,L,NC]

    # Block core source directories from public access
    RewriteRule ^(app|src|errors|tests|vendor|migrations|storage)/(.*)$ - [F,L,NC]

    # Explicitly block installer files and the .installed marker (defense in depth)
    RewriteRule ^app/install(/.*)?$ - [F,L,NC]
    RewriteRule ^\.installed$ - [F,L,NC]

    # Block direct PHP execution except index.php
    RewriteCond %{REQUEST_FILENAME} \.php$
    RewriteCond %{REQUEST_URI} !/index.php$
    RewriteRule ^ - [F,L,NC]

    # Front Controller Pattern
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ index.php?page=$1 [QSA,L]
</IfModule>
